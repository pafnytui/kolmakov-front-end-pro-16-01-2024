const getSum = () => {
    let num = 0;

    return mathOperation = (value) => {
        num += value;
        return num;
    }
};

let sum = getSum();

console.log(sum(3));

console.log(sum(5));

console.log(sum(20));